{
  "identifier": "default",
  "description": "Scoped capabilities for VRY: only the event IPC the frontend uses plus the opener plugin. Replaces the broad core:default grant.",
  "windows": ["main"],
  "permissions": [
    "core:event:default",
    "opener:default"
  ]
}
